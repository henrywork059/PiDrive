# PATCH NOTES — piTrainer_0_2_8

## Summary
This patch makes the dock title bars and buttons smaller again for a tighter dark-mode UI.

## Changed files
- `piTrainer/ui/styles.py`

## UI changes
- Reduced `QDockWidget::title` padding from `7px 11px` to `6px 10px`
- Reduced `QPushButton` minimum height from `27px` to `24px`
- Reduced `QPushButton` padding from `4px 9px` to `3px 8px`

## Notes
- This is a patch-only zip.
- Relative folder paths are kept the same as the project layout.
- No logic or wiring changes were made in this patch.
